# aidev-test-1

This repo is a sanity-check test project for the AI Dev Manager system, used to validate the end-to-end flow on a minimal codebase. The runnable example lives in `hello.py`, which you can execute directly to confirm the project works. If you'd like to contribute, see `CONTRIBUTING.md` for setup and contribution instructions.